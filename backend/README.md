# Backend — base preparada

Este backend es una **estructura inicial**, no reemplaza todavía el LocalStorage de la maqueta. Incluye una API mínima de salud y carpetas por dominio para conectar posteriormente una base de datos real.

La separación permite desplegar frontend y backend de forma independiente y mantener credenciales de base de datos fuera del navegador.
